# YumYum Agent 제품 기획서

- 문서 상태: 승인 범위 구현 중
- 제품명(가칭): **YumYum Agent**
- 작성 목적: 제품 정의, MVP 요구사항, 책임 경계, 검증 기준 및 확장 계획 확정
- 대상 플랫폼: macOS 우선
- 최소 지원: macOS 14 Sonoma
- 주요 검증 환경: macOS 26, Apple Silicon
- 문서 기준일: 2026-07-26
- 표기 규칙:
  - **확정**: 제품 결정이 완료된 요구사항
  - **TBD**: 구현 전 조사·검증 또는 추가 제품 결정이 필요한 항목

### 문서 통제

| 항목 | 값 |
|---|---|
| 문서 소유 | YumYum Agent 제품기획 |
| 제품 단계 | MVP 상세 기획 |
| 기준 문서 | 본 문서 |
| 변경 원칙 | 기존 제품 정의와 불변 조건을 유지하며 결정 로그를 통해 변경 |
| 구현 상태 | 로컬 에이전트 발견·빠른 메뉴 구현 |
| 배포 대상 | macOS 14·15·26, Apple Silicon |

### 결정 로그

| ID | 상태 | 결정 |
|---|---|---|
| D-001 | 확정 | MVP는 승인 후 실제 작업 1개까지 포함하며 대표 작업은 캘린더 일정 등록이다. |
| D-002 | 확정 | YumYum은 사용자 Mac에 설치된 Hermes, OpenCode, Codex, Claude Code CLI를 검증된 전용 로컬 Connector로 실행한다. 인증 토큰을 별도로 보관하지 않는다. |
| D-003 | 확정 | 자료를 먹이면 별도 지시 입력창 없이 즉시 처리를 시작한다. |
| D-004 | 확정 | 먹이기 동작을 현재 활성 창 관련 맥락 처리의 동의 트리거로 간주한다. 접근성 맥락은 활성 창으로 제한한다. |
| D-005 | 확정 | 일반 캡처·텍스트는 사용자가 명시적으로 선택한 기본 로컬 에이전트에 전달한다. 외부 변경은 실행 직전 현재 Task에 한정된 일회성 승인을 요구한다. |
| D-006 | 확정 | 자격증명·보안정보는 절대 전송하지 않는다. 일반 개인정보는 마스킹 미리보기와 경고 후 명시적 확인이 있어야 전송한다. |
| D-007 | 확정 | 입력 원본은 처리 중 임시 저장하고 완료·취소 후 즉시 삭제한다. 명시적으로 보관한 항목만 앱 전용 기록에 저장한다. |
| D-008 | 확정 | 최소 macOS 14, 권장·주요 검증 macOS 26, Apple Silicon 우선이다. Intel Mac은 MVP 공식 지원에서 제외한다. |
| D-009 | 확정 | MVP는 단일 실행과 FIFO 큐를 사용한다. 대기 작업은 개별 취소할 수 있고 현재 작업 종료 후 다음 작업을 자동 시작한다. |
| D-010 | 확정 | 한 번의 다중 선택 또는 다중 드롭만 하나의 Meal로 묶는다. 시간 기반 자동 병합은 하지 않는다. |
| D-011 | 확정 | Hermes, OpenCode, Codex, Claude Code를 안전한 고정 디렉터리와 사용자 명시 경로에서 자동 발견한다. 정의 ID와 정확 경로만 저장하며, 선택 경로가 unavailable이면 자동 폴백 없이 명시적 재선택을 요구한다. |
| D-012 | 확정 | 펫 클릭 또는 기본 `Control+Option+Space`로 비활성 말풍선 빠른 메뉴를 열고 ScreenCaptureKit 캡처, 대화 텍스트, NSOpenPanel 다중 파일 선택을 제공한다. 파일은 각각 최대 20MB다. |

### 용어

| 용어 | 정의 |
|---|---|
| 먹이기(Feed) | 사용자가 캡처, 붙여넣기, 드롭 또는 파일 선택으로 자료 처리를 명시적으로 시작하는 행위 |
| FoodItem | 먹인 개별 자료와 출처·민감도·추출 결과를 표현하는 단위 |
| Meal | 한 번의 사용자 입력 동작으로 들어온 하나 이상의 FoodItem 묶음 |
| Task | Meal 하나를 명시적으로 선택한 AgentConnector에 전달해 결과 또는 실행 완료까지 추적하는 작업 |
| 활성 창 맥락 | 먹이기 시점의 전면 앱과 현재 활성 창에서 접근성 API로 얻은 관련 정보 |
| AgentConnector | 외부 에이전트의 연결·요청·이벤트·취소를 공통 UI 계약으로 변환하는 계층 |
| Local CLI Connector | 검증된 정확 경로의 Hermes, OpenCode, Codex, Claude Code CLI를 전용 argv 계약으로 실행하는 Connector |
| 외부 변경 | 캘린더 등록, 이메일 전송, 파일 변경 등 사용자 환경이나 외부 시스템 상태를 바꾸는 작업 |
| 절대 차단 정보 | 사용자 승인으로도 MVP에서 외부 전송할 수 없는 자격증명·보안정보 |
| 일반 개인정보 | 마스킹 미리보기와 명시적 범위 확인 후 전송할 수 있는 개인정보 |

---

## 1. 제품 개요

YumYum Agent는 macOS 화면 우하단에 항상 머무는 초경량 AI 데스크톱 펫이다. 사용자는 화면에서 궁금한 부분을 캡처하거나, 복사한 텍스트와 이미지, 파일, 폴더, URL 등을 펫에게 ‘먹일’ 수 있다.

펫은 전달받은 자료를 오물오물 먹는 애니메이션으로 처리 상태를 보여주고, 분석이 끝나면 말풍선으로 답한다. 단순히 내용을 설명하는 데 그치지 않고 Hermes Agent 또는 Codex와 연결해 일정 등록, 이메일 작성, 파일 정리, 코드 수정, 테스트 실행 같은 실제 작업도 수행한다.

제품의 핵심은 귀여운 캐릭터 자체가 아니다. **macOS 어디서든 자료를 에이전트에게 전달할 수 있는 직관적인 범용 입력 인터페이스**가 본질이다. ‘화면의 정보를 먹인다’는 하나의 행동으로 캡처, 클립보드, 파일, 폴더, URL과 AI 에이전트를 연결한다.

### 한 문장 소개

> 화면에서 궁금한 것을 캡처하거나 끌어다 먹이면, 내용을 이해하고 답하거나 직접 처리해주는 AI 데스크톱 펫.

### 영문 소개

> Feed it anything on your screen. YumYum understands it and gets things done.

---

## 2. 해결하려는 문제

현재 AI 도구를 사용하려면 보통 다음 과정을 거쳐야 한다.

1. 필요한 내용을 복사한다.
2. AI 앱이나 브라우저를 연다.
3. 새 대화에 붙여넣는다.
4. 상황을 설명한다.
5. 답변을 받은 뒤 원래 앱으로 돌아간다.
6. 필요한 작업을 다시 사람이 수행한다.

이미지나 앱 화면은 과정이 더 길다. 스크린샷을 저장하고, 파일을 첨부하고, 화면에 무엇이 있는지 설명해야 한다. 코드 문제라면 별도의 코딩 에이전트를 다시 실행하고 프로젝트 위치와 오류 상황도 전달해야 한다.

YumYum Agent는 이 흐름을 다음처럼 줄인다.

1. 궁금한 화면을 캡처하거나 자료를 펫에게 드롭한다.
2. 펫이 자료와 사용자가 먹이기로 동의한 현재 활성 창 맥락을 읽는다.
3. 말풍선으로 답하거나 적절한 에이전트가 작업을 실행한다.

사용자는 AI 앱을 찾아다니지 않고, 현재 작업 흐름 안에서 바로 도움을 받을 수 있다.

---

## 3. 제품 콘셉트

### 3.1 핵심 메타포: 먹이기

사용자가 AI에게 정보를 전달하는 모든 행동을 ‘먹이기’로 통일한다.

- 화면 영역 캡처 → 화면 조각 먹이기
- 선택한 텍스트 → 문장 먹이기
- 클립보드 텍스트 → 복사한 내용 먹이기
- 클립보드 이미지 → 이미지 먹이기
- 파일 → 문서 먹이기
- 폴더 → 작업 묶음 먹이기
- URL → 웹페이지 먹이기

기술 용어를 몰라도 드래그 앤 드롭, 복사와 붙여넣기, 캡처처럼 익숙한 행동만으로 에이전트를 사용할 수 있어야 한다.

### 3.2 캐릭터의 역할

펫은 단순한 장식이 아니라 다음 역할을 동시에 담당한다.

- 항상 접근 가능한 AI 호출 버튼
- 파일과 클립보드의 드롭 대상
- 화면 캡처 시작 버튼
- 처리 상태를 보여주는 인디케이터
- 답변을 표시하는 말풍선의 기준점
- Hermes와 Codex를 연결하는 에이전트 인터페이스

### 3.3 제품 원칙

1. **항상 있지만 방해하지 않는다.**
2. **사용자가 명시적으로 먹인 자료만 처리한다.**
3. **답변보다 행동까지 이어지는 경험을 우선한다.**
4. **대기 상태에서는 CPU, 메모리, 배터리 사용량을 최소화한다.**
5. **민감한 파일과 화면 정보는 기본적으로 보호한다.**
6. **캐릭터의 귀여움이 기능을 가리지 않도록 한다.**
7. **YumYum 자체가 또 하나의 에이전트가 되지 않는다.**
8. **사용자가 이미 쓰는 에이전트를 교체하지 않고 화면 위로 가져온다.**

### 3.4 제품의 최상위 방향

> **YumYum은 에이전트가 아니라, 어떤 에이전트든 화면 위에서 사용할 수 있게 해주는 네이티브 macOS 인터페이스다.**

YumYum은 자체 추론 엔진, 장기 기억, 도구 실행 체계와 모델 제공자 관리 기능을 새로 만들지 않는다. 대신 macOS 화면과 사용자의 에이전트 사이를 연결하는 역할에 집중한다.

YumYum이 담당하는 영역:

- 화면 캡처와 영역 선택
- 선택한 텍스트와 클립보드 읽기
- 이미지, 파일, 폴더, URL 드래그 앤 드롭
- 먹이기 시점에 macOS 접근성 API를 통한 현재 활성 창 맥락 수집
- 입력 자료의 미리보기와 민감정보 필터링
- 펫 애니메이션과 말풍선 인터페이스
- 연결된 에이전트로 자료 및 요청 전달
- 에이전트의 진행 상황, 답변, 실행 결과 표시

연결된 에이전트가 담당하는 영역:

- 사용자 요청 해석과 추론
- 대화 세션과 장기 기억
- 웹 검색, 일정, 이메일, 파일 및 터미널 도구 실행
- 코드베이스 탐색과 코드 수정
- 외부 서비스 및 자동화 연동
- 모델과 제공자 선택

이 분리는 YumYum을 가볍게 유지하고, 특정 모델이나 에이전트 서비스에 종속되지 않게 한다. 사용자는 Hermes, Codex, OpenCode 또는 자신이 운영하는 에이전트를 그대로 사용하면서 YumYum의 화면 인터페이스만 추가할 수 있다.

### 3.5 독립 앱과 플러그인의 역할

YumYum 본체는 Swift와 AppKit으로 개발하는 독립형 macOS 앱으로 제공한다. Hermes Desktop 플러그인만으로 구현하지 않는다.

Hermes Desktop 플러그인은 앱 내부의 패널, 페이지, 상태 표시, 명령 등을 확장하는 데 적합하지만 다음과 같은 운영체제 수준의 경험에는 한계가 있다.

- 모든 앱 위에 떠 있는 투명한 펫
- 포커스를 빼앗지 않는 네이티브 플로팅 패널
- Finder의 파일과 폴더 직접 드롭
- 화면 영역 캡처
- macOS 클립보드와 접근성 API 사용
- 여러 모니터와 전체 화면 앱 대응

따라서 역할을 다음과 같이 구분한다.

```text
YumYum macOS 앱
├── 펫과 말풍선
├── 캡처·클립보드·파일·폴더 입력
├── OCR와 민감정보 필터
└── Agent Connector
    ├── Hermes
    ├── Codex CLI
    ├── OpenCode
    ├── OpenAI 호환 API
    └── 사용자 지정 Webhook

선택형 Hermes Desktop 플러그인
├── YumYum 작업 기록
├── 에이전트 연결 상태
├── 실행 결과와 세션 확인
└── 상세 설정 및 관리
```

---

## 4. 목표 사용자

### 4.1 주요 사용자

- 하루 종일 맥으로 문서, 메일, 웹, 메신저를 오가는 사용자
- AI를 자주 쓰지만 앱 전환과 복사·붙여넣기가 번거로운 사용자
- Hermes Agent 같은 개인비서 에이전트를 운영하는 사용자
- Codex, OpenCode 등 코딩 에이전트를 사용하는 개발자
- 이미지, PDF, 문서, 코드 등 다양한 형식의 자료를 자주 분석하는 사용자

### 4.2 대표 사용 상황

- 긴 문서 일부를 캡처해 즉시 요약한다.
- 외국어 문장을 복사한 뒤 펫에게 붙여넣어 번역한다.
- 이메일 본문을 먹이고 답장 초안을 만든다.
- 일정 안내 이미지를 먹이고 캘린더에 등록한다.
- 오류 메시지를 캡처해 원인과 해결 방법을 묻는다.
- 프로젝트 폴더를 먹이고 Codex에 수정 작업을 맡긴다.
- PDF와 스프레드시트를 함께 먹여 내용이 일치하는지 확인한다.
- 웹페이지 URL을 먹이고 핵심 내용만 정리한다.

---

## 5. 핵심 사용자 경험

### 5.1 기본 상태

- 펫은 화면 우하단에 작은 크기로 머문다.
- 기본 크기는 약 48~80px 범위에서 조절할 수 있다.
- 배경과 테두리가 없는 투명 창을 사용한다.
- Dock에는 표시하지 않는다.
- 평소에는 정지하거나 낮은 빈도로 눈을 깜빡인다.
- 사용자가 원하는 위치로 이동하고 위치를 고정할 수 있다.

### 5.2 화면 캡처 먹이기

1. 사용자가 펫을 클릭한다.
2. 빠른 메뉴에서 `화면 캡처`를 선택하거나, 기본 동작으로 바로 캡처 모드에 진입한다.
3. 화면이 살짝 어두워지고 사용자가 영역 또는 창을 선택한다.
4. 선택한 화면 조각이 작아지며 펫에게 날아간다.
5. 펫이 화면 조각을 받아먹는다.
6. 처리 중에는 오물오물 씹는 애니메이션이 재생된다.
7. 분석이 끝나면 말풍선으로 답변이 나타난다.
8. 사용자는 답변을 복사하거나 후속 작업을 실행한다.

### 5.3 파일 및 폴더 먹이기

1. 사용자가 Finder에서 파일이나 폴더를 드래그한다.
2. 포인터가 펫 근처에 오면 펫이 입을 벌리고 드롭 가능 상태를 표시한다.
3. 자료를 놓으면 파일 아이콘 또는 폴더 아이콘이 펫에게 흡수된다.
4. 펫이 파일 형식, 크기, 권한 및 위험 요소를 확인한다.
5. 전송 대상, 제외 항목, 크기와 위험 요소를 표시하고 사용자에게 범위 확인을 요청한다.
6. 사용자가 확인하면 안전한 범위를 명시적으로 선택한 기본 로컬 Connector에 전달한다.

예시:

> `duncan-dashboard` 폴더를 먹었어. 코드 파일 124개를 찾았고, 비밀 파일 3개는 제외했어. 프로젝트를 분석할까?

### 5.4 클립보드 먹이기

지원 동작:

- 펫을 선택한 상태에서 `⌘V`
- 펫 메뉴의 `클립보드 먹이기`
- 전역 단축키로 즉시 먹이기

클립보드 형식을 판별해 다음처럼 처리한다.

- 일반 텍스트: 요약, 번역, 교정, 질문, 답장 작성
- 이미지: 시각 분석, OCR, 번역, UI 분석
- 파일 URL: 파일 또는 폴더 분석
- 웹 URL: 페이지 내용 읽기 및 요약
- 여러 항목: 하나의 작업 묶음으로 처리

### 5.5 답변 말풍선

말풍선은 펫 위쪽 또는 화면 여유 공간에 표시한다.

기본 구성:

- 스트리밍 답변
- 입력 자료 미리보기
- 처리 중인 에이전트 표시
- `복사`
- `더 물어보기`
- `실행`
- `Codex로 열기`
- `Hermes에게 맡기기`
- `닫기`

긴 답변은 말풍선에 전부 표시하지 않는다. 핵심 답변과 다음 행동만 보여주고, 필요하면 확장 패널에서 전체 내용을 확인한다.

---

## 6. 먹이 데이터 모델

모든 입력을 공통 `FoodItem` 구조로 정리한다.

```text
FoodItem
├── id
├── type
│   ├── screenshot
│   ├── image
│   ├── selectedText
│   ├── clipboardText
│   ├── file
│   ├── folder
│   └── url
├── sourceApplication
├── sourceWindowTitle
├── localReference
├── extractedText
├── mimeType
├── createdAt
├── sensitivity
└── systemInstruction
```

여러 항목을 동시에 전달할 때는 `Meal` 단위로 묶는다.

```text
Meal
├── items: [FoodItem]
├── systemInstruction
├── selectedConnector
├── privacyPolicy
└── conversationContext
```

예를 들어 PDF, 엑셀 파일과 “두 자료의 수치를 비교해줘”라는 문장을 하나의 Meal로 전달할 수 있다.

---

## 7. 에이전트 연동

### 7.1 Hermes Agent

Hermes는 생활 및 업무 전반을 처리하는 기본 개인비서 엔진으로 사용한다.

주요 작업:

- 문서 요약 및 번역
- 웹 검색
- 이메일 작성 및 전송
- 캘린더 일정 등록
- 파일 생성 및 정리
- 메모 저장
- 외부 서비스 및 자동화 호출
- 장기 대화와 개인화된 응답

연결 방식:

- 사용자 Mac에 설치되고 로그인된 `hermes` CLI를 로컬 자식 프로세스로 직접 실행
- YumYum은 Hermes 인증 토큰을 별도로 보관하지 않음
- 구조화 출력, 스트리밍, 세션, 승인, 취소 지원 여부는 공식 문서와 실제 CLI에서 검증
- 실행 작업 전 YumYum의 별도 승인 단계 제공

### 7.2 Codex

Codex는 개발 작업에 특화된 엔진으로 사용한다.

주요 작업:

- 오류 메시지 분석
- 코드 설명 및 리뷰
- 프로젝트 파일 탐색
- 코드 수정
- 테스트 및 빌드 실행
- 변경사항 diff 생성

폴더를 먹였을 때 사용자가 허용하면 해당 폴더를 Codex 작업 디렉터리로 연결한다. 단순 캡처만 전달할지 실제 프로젝트 접근까지 허용할지는 명확히 구분한다.

### 7.3 명시적 기본 에이전트와 자동 라우팅

MVP는 검증된 Hermes, OpenCode, Codex, Claude Code 중 사용자가 기본 에이전트를 명시적으로 선택한다. 선택한 정확 경로가 unavailable이면 전송을 막고 재선택을 요구하며 다른 설치나 정의로 자동 폴백하지 않는다.

> 입력 내용 기반 자동 라우팅은 MVP 이후 범위다.

`자동 선택` 모드에서는 입력 내용과 사용자 요청을 기준으로 엔진을 고른다.

- 코드, 터미널 오류, 프로젝트 폴더 → Codex 우선
- 일정, 이메일, 문서, 검색, 개인 업무 → Hermes 우선
- 단순 이미지 분석이나 질의 → 설정된 기본 모델

자동 선택 결과는 사용자에게 표시하고 언제든 바꿀 수 있게 한다.

### 7.4 Agent Connector 구조

YumYum은 특정 에이전트의 내부 구현에 직접 결합하지 않는다. 모든 에이전트 연결은 공통 `AgentConnector` 계층을 통과한다.

개념적인 인터페이스는 다음과 같다.

```swift
protocol AgentConnector {
    var id: String { get }
    var displayName: String { get }
    var capabilities: Set<AgentCapability> { get }

    func checkConnection() async throws -> ConnectionStatus
    func send(_ meal: Meal) -> AsyncThrowingStream<AgentEvent, Error>
    func cancel(taskID: String) async
}
```

공통 이벤트 예시:

```text
AgentEvent
├── connected
├── thinking
├── textDelta
├── toolRequested
├── approvalRequired
├── toolStarted
├── toolFinished
├── artifactCreated
├── completed
└── failed
```

말풍선과 펫 애니메이션은 에이전트별 응답 형식이 아니라 이 공통 이벤트만 사용한다. 새로운 에이전트를 추가할 때 네이티브 UI를 다시 만들지 않고 Connector만 구현할 수 있다.

### 7.5 초기 지원 우선순위

1. **Hermes ACP Connector**: 설치된 로컬 도움말에서 ACP 계약이 확인된 경우에만 사용
2. **OpenCode CLI Connector**: 확인된 `run`·`--file` 계약 사용
3. **Codex CLI Connector**: 확인된 `exec`·`--image` 계약과 read-only sandbox 사용
4. **Claude Code CLI Connector**: 확인된 print·plan 계약 사용
5. **사용자 지정 Webhook·OpenAI 호환 API Connector**: MVP 이후 검토

MVP는 네 로컬 CLI의 발견·명시적 선택·재검증·무폴백과 캡처/텍스트/파일 전달을 동일한 공통 요청 흐름으로 검증한다. 광범위한 플러그인이나 사용자 명령 구성은 추가하지 않는다.

### 7.6 Codex 연결 원칙

YumYum이 Codex 계정 인증이나 개인 구독 토큰을 직접 보관하지 않는다. 사용자 맥에 설치되고 로그인된 Codex CLI를 로컬 프로세스로 호출하는 방식을 우선 검토한다.

프로젝트 폴더를 먹였을 때:

1. 폴더 접근 범위를 표시한다.
2. 사용자에게 Codex 작업 전환을 확인한다.
3. 해당 폴더를 작업 디렉터리로 세션을 시작한다.
4. 요청과 캡처 자료를 전달한다.
5. 진행 상황, 명령 실행, 변경사항과 diff를 표시한다.
6. 위험한 작업이나 권한 확대가 필요하면 별도로 승인을 받는다.

---

## 8. 펫 상태와 애니메이션

### 8.1 기본 상태

| 상태 | 의미 | 표현 예시 |
|---|---|---|
| idle | 대기 | 정지, 눈 깜빡임 |
| hover | 사용자 접근 | 사용자를 바라봄 |
| ready | 입력 대기 | 입을 벌림 |
| catch | 자료 받기 | 화면 조각이나 파일을 잡음 |
| chew | 분석 중 | 오물오물 씹기 |
| think | 에이전트 처리 중 | 고민하는 표정 |
| answer | 응답 완료 | 말풍선 표시 |
| action | 작업 실행 중 | 바쁘게 움직임 |
| success | 작업 완료 | 만족스러운 반응 |
| error | 처리 실패 | 뱉거나 고개를 갸웃함 |
| blocked | 위험 자료 차단 | 놀라며 거부함 |

### 8.2 입력 유형별 표현

- 텍스트: 종이를 돌돌 말아 먹는다.
- 이미지: 사진 조각을 한입에 먹는다.
- URL: 링크를 면처럼 후루룩 먹는다.
- 파일: 파일 아이콘을 씹는다.
- 폴더: 큰 폴더를 힘겹게 삼킨다.
- 여러 항목: 차례대로 먹고 개수를 표시한다.
- 위험 파일: 즉시 뱉고 경고 말풍선을 표시한다.

### 8.3 구현 전략

MVP에서는 Live2D를 사용하지 않는다. 투명 PNG/WebP 프레임 또는 Core Animation 기반의 단순한 2D 애니메이션으로 시작한다.

필요한 스킨과 확대·축소 요구가 커지면 Rive 기반 벡터 애니메이션을 검토한다. 상시 60fps 애니메이션은 피하고, 대기 상태에서는 이벤트 기반 또는 1~2fps 수준으로 움직임을 제한한다.

---

## 9. 기술 구성

### 9.1 권장 스택

| 영역 | 권장 기술 |
|---|---|
| 언어 | Swift |
| 상시 펫 창 | AppKit `NSPanel` |
| 펫 애니메이션 | Core Animation `CALayer` |
| 설정 화면 | SwiftUI |
| 화면 캡처 | ScreenCaptureKit |
| 화면 텍스트 접근 | macOS Accessibility API |
| 이미지 OCR | Apple Vision Framework |
| 클립보드 | `NSPasteboard` |
| 파일·폴더 드롭 | AppKit Drag and Drop API |
| 파일 미리보기 | Quick Look |
| 네트워크 | URLSession + SSE/WebSocket |
| 설정 저장 | UserDefaults 또는 로컬 데이터베이스 |
| 로그인 시 실행 | ServiceManagement Login Item API |

### 9.2 AppKit을 사용하는 이유

이 제품은 일반적인 네모난 앱 창보다 macOS 창 제어가 중요하다.

- 배경이 투명한 작은 창
- 다른 앱 위에 항상 표시
- Dock 아이콘 없이 실행
- 활성 앱의 포커스를 빼앗지 않는 패널
- 동물 모양에 맞춘 클릭 영역
- 여러 모니터와 전체 화면 앱 대응
- 드래그 앤 드롭 처리
- 말풍선 위치 정밀 제어

따라서 상시 표시되는 펫과 말풍선은 AppKit으로 만들고, 설정 화면만 SwiftUI를 사용하는 혼합 구조가 적합하다.

### 9.3 개발 환경

Xcode IDE를 항상 사용할 필요는 없다.

- 코드 작성: Orca와 Codex/OpenCode 또는 원하는 에디터
- 프로젝트 관리: Swift Package Manager 또는 Xcode 프로젝트
- 빌드 및 테스트: 터미널
- 실제 실행 확인: macOS
- 서명, 권한, 배포: 필요할 때 Xcode 사용

AppKit은 macOS 전용이므로 실제 빌드와 실행은 맥에서 해야 한다. Ubuntu 서버에서는 코드 작성과 백엔드 개발은 가능하지만 macOS 앱 빌드는 할 수 없다.

---

## 10. 성능 목표

YumYum Agent는 항상 떠 있어야 하므로 대기 상태의 자원 사용량이 중요하다.

초기 목표:

- 대기 CPU: 사실상 0%에 가까운 수준
- 대기 메모리: 약 30~70MB 목표
- 대기 네트워크: 없음
- 화면 캡처: 사용자가 요청할 때만
- OCR 및 비전 분석: 입력이 있을 때만
- 대기 애니메이션: 정지 또는 낮은 빈도

상시 프로세스에는 웹뷰, 로컬 LLM, OCR 엔진, 화면 녹화 기능을 올려두지 않는다. 입력이 들어올 때만 필요한 기능을 실행하고 작업이 끝나면 이미지와 분석 객체를 메모리에서 해제한다.

Electron은 메모리와 상시 자원 사용량 측면에서 우선 제외한다. 크로스 플랫폼보다 macOS 네이티브 경험과 경량성을 먼저 확보한다.

---

## 11. 개인정보 보호와 안전

### 11.1 기본 원칙

- 사용자가 명시적으로 먹인 자료만 처리한다.
- 먹이기 동작은 해당 시점의 현재 활성 창 관련 맥락 처리에 대한 동의 트리거다.
- 기본 설정에서는 화면을 상시 녹화하지 않는다.
- 캡처 이미지와 임시 파일은 처리 후 삭제한다.
- 외부 모델로 전송되는 자료 범위를 전송 전에 알 수 있게 한다.
- 민감한 파일은 자동 탐지하고 기본 차단한다.

### 11.2 폴더 처리 안전장치

폴더를 먹였을 때 내부 파일을 무조건 전송하지 않는다.

1. 로컬에서 파일 목록을 확인한다.
2. `.git`, `node_modules`, 빌드 결과물, 캐시를 기본 제외한다.
3. `.env`, 인증서, 개인 키, 토큰 파일을 차단한다.
4. 파일 종류, 개수, 전체 크기를 표시한다.
5. 필요한 파일만 선별한다.
6. 외부 전송 전 처리 범위를 확인한다.

### 11.3 권한

필요할 수 있는 macOS 권한:

- 화면 기록 또는 캡처 권한
- 접근성 권한
- 선택한 파일과 폴더 접근 권한
- 마이크 권한(향후 음성 기능 사용 시)
- 로그인 시 실행 권한

권한은 기능을 처음 사용할 때 요청하고, 앱 실행 직후 모든 권한을 한꺼번에 요구하지 않는다.

접근성 권한이 없으면 먹인 자료만 처리한다. 사용자는 설정에서 활성 창 맥락 자동 수집을 끌 수 있다.

---

## 12. MVP 범위

### 12.1 반드시 포함

1. macOS 우하단 투명 펫
2. 항상 위 표시 및 위치 이동
3. 펫 클릭 후 영역 캡처
4. 캡처가 펫에게 날아가는 애니메이션
5. 오물오물 처리 애니메이션
6. Hermes, OpenCode, Codex, Claude Code 전용 로컬 Connector 연결
7. 스트리밍 말풍선 답변
8. 파일 및 폴더 드래그 앤 드롭
9. 클립보드 텍스트 및 이미지 붙여넣기
10. 답변 복사 및 후속 질문
11. 기본 개인정보 보호 설정
12. 로그인 시 자동 실행

### 12.2 MVP에서 제외

- 상시 화면 녹화와 장기 화면 기억
- Live2D 및 3D 캐릭터
- 복잡한 펫 성장 시스템
- 펫 간 소셜 기능
- Windows 및 Linux 지원
- 음성 호출 및 상시 마이크 대기
- Mac App Store 정식 배포
- 플러그인 마켓

초기 버전은 ‘먹이고 답을 받으며, 캘린더 일정 등록안을 승인해 실제 등록한다’는 핵심 경험이 즐겁고 빠른지 검증하는 데 집중한다.

---

## 13. 주요 화면 및 UI

### 13.1 펫

- 화면 우하단 기본 배치
- 투명 배경
- 클릭, 드롭, 붙여넣기 지원
- 드래그로 위치 이동
- 클릭 또는 사용자 지정 전역 단축키로 빠른 메뉴 열기

### 13.2 빠른 메뉴

- ScreenCaptureKit 화면·창 선택
- 직접 대화 텍스트 입력
- 파일 선택
- 에이전트 선택
- 설정

### 13.3 말풍선

- 핵심 답변
- 입력 미리보기
- 처리 엔진
- 후속 질문 입력
- 복사 및 실행 버튼

### 13.4 설정

- 네 로컬 CLI의 검증 상태, 정확 경로와 버전
- 명시적으로 선택한 기본 에이전트와 재선택 상태
- 펫 종류와 크기
- 기본 위치와 모니터
- 항상 위 표시 범위
- 전역 단축키
- 캡처 및 파일 보관 정책
- 민감 파일 제외 규칙
- 로그인 시 자동 실행

---

## 14. 후속 버전 아이디어

### 14.1 장기 화면 기억

Screenpipe 같은 로컬 화면 기록 도구와 선택적으로 연결해 다음 질문을 지원할 수 있다.

- “아까 본 상품 다시 찾아줘.”
- “오늘 오전에 읽은 문서가 뭐였지?”
- “지난주 회의에서 나온 일정만 정리해줘.”

기본 기능으로 강제하지 않고 사용자가 명시적으로 활성화하는 선택 기능으로 제공한다.

### 14.2 능동적 제안

사용자가 허용한 범위에서 펫이 다음 작업을 제안할 수 있다.

- 오류 화면 감지 후 도움 제안
- 일정 정보가 포함된 메시지에서 캘린더 등록 제안
- 긴 문서에서 요약 제안
- 반복 작업 감지 후 자동화 제안

능동 제안은 방해가 될 수 있으므로 빈도와 앱별 허용 목록을 제공한다.

### 14.3 펫 확장

- 여러 동물 스킨
- 사용자 지정 이름
- 작업 상태에 따른 감정 표현
- 사용 빈도에 따른 작은 성장 요소
- 커뮤니티 펫 스킨

펫 요소는 기능 사용을 즐겁게 만드는 수준으로 유지하고, 과도한 게임화는 피한다.

---

## 15. 제품명과 언어 표현

### 제품명

- 정식 후보: **YumYum**
- 설명형 명칭: **YumYum Agent**
- 펫 이름: 사용자가 직접 지정 가능

### 영어 표현

- 먹이기: `Feed YumYum`
- 처리 중: `YumYum is munching…`
- 처리 완료: `YumYum got it!`
- 처리 실패: `YumYum couldn't digest this.`
- 여러 항목: `YumYum ate 3 items.`

### 한국어 표현

- “얌얌이에게 먹여줘.”
- “오물오물 분석 중…”
- “내용을 이해했어.”
- “이 파일은 먹기 전에 확인이 필요해.”
- “민감한 파일이 있어서 제외했어.”

### 태그라인 후보

- **Feed your screen to your agent.**
- **Capture it. Drop it. YumYum handles it.**
- **Anything on your screen, now actionable.**
- **궁금한 건 얌얌이에게 먹여줘.**
- **캡처하고, 끌어놓고, 맡겨두세요.**

---

## 16. 차별점

기존 도구는 대체로 다음 중 하나에 집중한다.

- 화면 캡처 후 AI에게 질문
- 회의 음성 인식과 실시간 조언
- 데스크톱 캐릭터와 음성 대화
- 장기 화면 기록과 검색
- 코딩 에이전트 실행

YumYum Agent는 다음을 하나의 행동으로 결합한다.

1. 화면, 클립보드, 파일, 폴더, URL을 모두 받는다.
2. 펫에게 ‘먹인다’는 직관적인 인터랙션을 제공한다.
3. 처리 과정을 캐릭터 애니메이션으로 보여준다.
4. Hermes와 Codex를 목적에 따라 연결한다.
5. 답변뿐 아니라 실제 작업 실행까지 이어진다.
6. 상시 실행되는 macOS 앱이지만 가볍게 동작한다.

핵심 경쟁력은 캐릭터가 아니라 **AI 에이전트의 입력 장벽을 없애는 데스크톱 인터페이스**다.

---

## 17. 성공 판단 기준

MVP에서는 다음 질문에 답해야 한다.

- 사용자가 AI 앱을 직접 여는 횟수가 줄어드는가?
- 캡처 또는 드롭 후 첫 답변까지 충분히 빠른가?
- 펫의 애니메이션이 대기 시간을 덜 지루하게 만드는가?
- 먹이기라는 동작을 별도 설명 없이 이해하는가?
- 답변에서 실제 작업 실행으로 자연스럽게 이어지는가?
- 하루 종일 실행해도 배터리와 메모리 부담이 적은가?
- 민감한 화면과 파일을 다룰 때 사용자가 통제감을 느끼는가?

초기 측정 항목:

- 일일 먹이기 횟수
- 입력 유형별 사용 비율
- 캡처 후 첫 답변까지 걸린 시간
- 답변 후 실행 버튼 사용률
- 작업 성공 및 취소 비율
- 대기 상태 CPU 및 메모리
- 권한 요청 단계별 이탈률

---

## 18. 개발 단계 제안

### 1단계: 인터랙션 프로토타입

- 투명 펫 창
- 클릭 및 드래그 이동
- 영역 캡처
- 화면 조각을 먹는 애니메이션
- 가짜 응답 말풍선

목표: 먹이기 경험이 직관적이고 즐거운지 확인한다.

### 2단계: 실제 AI 연결

- 로컬 Hermes CLI 연결
- 이미지 및 텍스트 전달
- 스트리밍 답변
- 후속 질문
- 복사 기능

목표: 실제 일상에서 반복 사용 가능한지 확인한다.

### 3단계: 범용 입력

- 클립보드 텍스트 및 이미지
- 파일과 폴더 드롭
- URL 처리
- 입력 묶음 Meal
- 민감 파일 차단

목표: AI에게 자료를 전달하는 기본 창구가 될 수 있는지 확인한다.

### 4단계: 실행형 에이전트

- Hermes 작업 실행
- Codex 프로젝트 연결
- 자동 라우팅
- 실행 전 확인 및 결과 표시

목표: 질문 도구에서 실제 개인비서로 확장한다.

### 5단계: 제품화

- 온보딩
- 권한 안내
- 로그인 시 실행
- 자동 업데이트
- 서명 및 공증
- 성능 최적화
- 오류 및 개인정보 보호 체계

---

## 19. 예상 위험과 대응

### 화면 및 접근성 권한에 대한 거부감

- 필요한 기능을 사용할 때만 권한을 요청한다.
- 왜 필요한지 실제 화면 예시로 설명한다.
- 상시 녹화하지 않는다는 점을 분명히 알린다.

### 캐릭터가 업무를 방해할 가능성

- 크기, 투명도, 위치, 애니메이션 빈도를 조절한다.
- 특정 앱 또는 전체 화면 모드에서 자동 숨김을 제공한다.
- 클릭 통과 모드를 제공한다.

### 대형 폴더 처리 비용

- 먼저 로컬에서 구조만 확인한다.
- 제외 규칙과 크기 제한을 둔다.
- 전체를 전송하지 않고 필요한 파일만 에이전트가 선택한다.

### 외부 에이전트 연결 복잡도

- 네 지원 CLI마다 설치된 로컬 `--help`에서 확인한 전용 Connector만 사용한다.
- 선택 경로가 unavailable이면 자동 폴백하지 않는다.
- 연결 상태, 정확 경로, 버전과 unavailable 이유를 설정 화면에서 명확히 보여준다.

### 제품이 장난감처럼 보일 가능성

- 귀여운 애니메이션과 실제 업무 실행 능력을 함께 보여준다.
- 말풍선은 간결하게 유지한다.
- 펫 스킨 대신 기능 중심의 데모를 우선한다.

---

## 20. 최종 제품 정의

> **YumYum은 에이전트가 아니라, 어떤 에이전트든 화면 위에서 사용할 수 있게 해주는 네이티브 macOS 인터페이스다.**

YumYum Agent는 macOS 우하단에 항상 떠 있는 초경량 AI 데스크톱 펫이다. 사용자는 화면 캡처, 텍스트, 이미지, 파일, 폴더, URL을 펫에게 먹일 수 있다. 펫은 자료를 먹고 처리하는 과정을 애니메이션으로 보여주고, 사용자가 연결한 Hermes, Codex, OpenCode 또는 다른 에이전트에 자료와 요청을 전달한다.

YumYum은 사용자의 기존 에이전트를 대체하지 않는다. 에이전트가 가진 모델, 기억, 도구와 실행 능력을 macOS의 모든 앱 위에서 즉시 사용할 수 있게 만든다. 자체 에이전트 기능을 새로 구축하는 대신 캡처, 클립보드, 파일 전달, 권한 관리, 처리 상태와 결과 표현에 집중한다.

제품이 성공하려면 다음 네 가지가 동시에 만족돼야 한다.

1. **먹이기가 복사·붙여넣기보다 빠르고 자연스러울 것**
2. **하루 종일 켜두어도 느껴지지 않을 만큼 가벼울 것**
3. **답변에서 끝나지 않고 사용자의 일을 실제로 처리할 것**
4. **사용자가 원하는 에이전트를 연결하고 교체할 수 있을 것**

YumYum의 정체성은 ‘귀여운 AI 캐릭터’나 ‘또 하나의 AI 에이전트’가 아니다.

> **화면 속 무엇이든 내가 쓰는 에이전트에게 먹일 수 있는 macOS 인터페이스.**

---

## 21. 상세 제품 정의와 불변 조건

### 21.1 제품 정의

YumYum은 사용자가 현재 작업 중인 macOS 화면에서 자료를 명시적으로 먹이면, 해당 자료를 사용자가 선택한 검증된 로컬 AgentConnector에 전달하고 답변·승인·실행 결과를 네이티브 UI로 보여주는 connector UI다.

MVP의 대표 세로 흐름은 일정 정보가 포함된 캡처·텍스트·이미지를 먹이고, Hermes가 생성한 일정 등록안을 사용자가 검토·승인한 뒤 캘린더에 실제 등록하는 것이다.

### 21.2 불변 조건

| ID | 불변 조건 |
|---|---|
| INV-001 | YumYum은 네이티브 macOS 독립 앱이다. |
| INV-002 | 상시 펫과 말풍선은 Swift와 AppKit으로 구현한다. 설정 화면에는 SwiftUI를 사용할 수 있다. |
| INV-003 | YumYum은 자체 에이전트, 추론 엔진, 장기 기억, 모델 제공자 관리 또는 도구 실행 체계를 만들지 않는다. |
| INV-004 | 사용자의 먹이기 행위가 자료와 현재 활성 창 맥락 처리의 동의 트리거다. 먹이기 없는 상시 수집은 하지 않는다. |
| INV-005 | MVP의 공식 로컬 연동은 Hermes, OpenCode, Codex, Claude Code이며 각 설치의 공식 로컬 도움말 계약을 검증한 경우에만 실행한다. |
| INV-006 | 외부 변경은 답변 생성과 분리하며 실행 직전 사용자의 명시적 승인을 받는다. |
| INV-007 | 자격증명과 보안정보는 사용자 승인으로도 전송하지 않는다. |
| INV-008 | 대기 상태의 CPU·메모리·배터리·네트워크 사용을 최소화한다. |
| INV-009 | 캐릭터는 입력·상태·결과를 표현하지만 기능과 접근성을 가리지 않는다. |
| INV-010 | Connector를 교체해도 펫과 말풍선 UI의 핵심 흐름은 유지된다. |

---

## 22. 목표와 비목표

### 22.1 목표

| ID | 목표 |
|---|---|
| GOAL-001 | 사용자가 현재 앱을 벗어나지 않고 화면·클립보드·파일·폴더·URL을 에이전트에 전달한다. |
| GOAL-002 | 별도 프롬프트 입력 없이 먹이기 즉시 유용한 분석 또는 등록안을 받는다. |
| GOAL-003 | 캘린더 일정 등록을 통해 답변에서 실제 작업 실행까지 이어지는 경험을 검증한다. |
| GOAL-004 | 전송 범위와 외부 변경을 사용자가 이해하고 통제한다. |
| GOAL-005 | 하루 종일 실행해도 사용자가 자원 부담과 업무 방해를 체감하지 않게 한다. |
| GOAL-006 | Hermes의 연결·진행·승인·결과를 AgentConnector 공통 계약으로 표현한다. |

### 22.2 비목표

| ID | MVP 비목표 |
|---|---|
| NOGOAL-001 | 자체 모델 호출, 도구 선택, 에이전트 계획 및 장기 기억 |
| NOGOAL-002 | 먹이기 없이 수행되는 상시 화면 녹화·분석·능동 제안 |
| NOGOAL-003 | Webhook, OpenAI 호환 API, 임의 플러그인·사용자 명령 Connector 지원 |
| NOGOAL-004 | Windows, Linux 및 Intel Mac 공식 지원 |
| NOGOAL-005 | Live2D, 3D, 성장·소셜 시스템 및 스킨 마켓 |
| NOGOAL-006 | 음성 호출과 상시 마이크 대기 |
| NOGOAL-007 | 원본 자동 보관 기간과 영구 민감정보 예외 규칙 |
| NOGOAL-008 | 시간 기반 Meal 자동 병합과 다중 작업 동시 실행 |

---

## 23. 페르소나와 JTBD

### 23.1 1차 페르소나: Hermes를 사용하는 지식 근로자

- 문서, 메일, 브라우저, 메신저, 캘린더를 하루 종일 오간다.
- AI를 반복 사용하지만 앱 전환과 맥락 설명을 번거롭게 느낀다.
- 일정 안내 화면이나 메시지를 캘린더에 옮기는 일을 자주 한다.
- 편의성만큼 외부 전송 범위와 실행 통제권을 중요하게 생각한다.

**JTBD**

> 작업 중 발견한 화면이나 자료를 현재 흐름을 깨지 않고 Hermes에 전달하고, 일정 같은 실제 작업을 확인 후 끝내고 싶다.

### 23.2 2차 페르소나: 코딩 에이전트 사용자

- 오류 화면과 프로젝트 폴더를 Codex 또는 OpenCode에 빠르게 전달하고 싶다.
- 폴더 접근 범위, 명령 실행, diff와 위험 작업 승인을 명확히 구분하고 싶다.
- MVP에서는 Hermes 흐름의 확장 가능성을 검토하는 후속 대상이다.

### 23.3 핵심 사용자 성과

- AI 앱을 직접 여는 횟수가 줄어든다.
- 화면을 설명하는 프롬프트 작성이 줄어든다.
- 일정 등록의 전사·입력 오류가 줄어든다.
- 전송과 실행 단계가 분리되어 통제감을 유지한다.

---

## 24. 핵심 사용자 여정

### UJ-001 최초 실행과 로컬 에이전트 선택

1. 사용자가 YumYum을 실행한다.
2. YumYum은 안전한 고정 디렉터리와 사용자 명시 경로에서 네 지원 CLI의 실행 가능 여부, 버전과 로컬 도움말 계약을 확인한다.
3. 모든 확인 결과의 상태·정확 경로·버전을 표시하고 사용자가 기본 에이전트를 선택한다.
4. 선택 경로가 unavailable이면 전송을 막고 명시적 재선택을 요구한다.
5. 화면 캡처 또는 자동 맥락 수집을 처음 사용할 때만 관련 macOS 권한을 요청한다.

**예외**

- 지원 CLI 미설치: 상태와 경로 안내만 제공하며 임의 설치하지 않는다.
- CLI 로그인 필요: 각 CLI의 공식 로그인 절차로 안내하며 토큰 입력을 받거나 저장하지 않는다.
- 접근성 권한 거부: 먹인 자료만 처리하고 자동 맥락은 생략한다.

### UJ-002 일반 캡처·텍스트 먹이기

1. 사용자가 캡처하거나 클립보드 텍스트를 붙여넣는다.
2. YumYum은 활성 창 맥락 수집이 켜져 있고 권한이 있으면 허용 범위의 맥락을 수집한다.
3. 절대 차단 정보와 일반 개인정보를 검사한다.
4. 민감정보가 없으면 지시 입력 없이 즉시 Task를 생성한다.
5. 실행 중인 작업이 없으면 시작하고, 있으면 FIFO 큐에 넣는다.
6. Hermes 이벤트를 펫 상태와 말풍선에 표시한다.
7. 완료 후 원본을 삭제하고 결과 요약만 기록한다.

### UJ-003 파일·폴더·대용량 자료 먹이기

1. 사용자가 한 번의 선택 또는 드롭으로 자료를 넣는다.
2. 같은 동작으로 들어온 자료만 하나의 Meal로 묶는다.
3. YumYum은 로컬에서 형식·개수·크기·제외 항목·민감정보를 확인한다.
4. 전송 가능한 범위의 미리보기를 보여준다.
5. 사용자가 범위를 확인하면 Task를 생성한다.
6. 취소하면 전송하지 않고 임시 원본을 삭제한다.

### UJ-004 큐와 취소

1. 현재 Task가 실행 중일 때 새 Meal이 들어오면 큐 끝에 추가한다.
2. 대기 항목은 순서와 입력 유형을 표시한다.
3. 사용자는 대기 항목을 개별 취소할 수 있다.
4. 현재 Task가 완료·실패·취소되면 다음 대기 Task를 자동 시작한다.
5. 현재 Task 취소는 Connector에 전달하고 로컬 처리를 중단한다.

### UJ-005 캘린더 일정 등록

1. 일정 정보가 포함된 자료를 먹인다.
2. Hermes가 자료를 해석해 일정 등록안을 반환한다.
3. YumYum은 제목, 시작·종료 시각, 시간대, 캘린더, 장소, 메모를 승인 화면에 표시한다.
4. 필수 값이 불명확하면 등록하지 않고 Hermes의 확인 질문을 표시한다.
5. 사용자가 승인하면 Hermes에 실행 승인을 전달한다.
6. Hermes 실행 결과를 수신해 성공 또는 실패를 표시한다.
7. 승인 거부 시 등록하지 않고 Task를 취소 또는 답변 완료 상태로 종료한다.

### UJ-006 권한·연결 실패 복구

- 화면 캡처 권한 없음: 시스템 설정 이동 안내와 재시도 제공
- 접근성 권한 없음: 먹인 자료만으로 계속 처리 가능
- 선택한 로컬 AgentConnector 실패: 다른 에이전트로 폴백하거나 원격 전송하지 않고 오류와 명시적 재선택 제공
- 스트림 중단: 실패 상태와 재시도 제공. 중복 실행 가능성이 있는 외부 변경은 상태 확인 전 자동 재시도하지 않음

---

## 25. 기능 요구사항

### 25.1 펫·입력·말풍선

| ID | 우선순위 | 요구사항 |
|---|---|---|
| FR-001 | Must | 투명한 AppKit 패널로 펫을 항상 접근 가능한 위치에 표시한다. |
| FR-002 | Must | 펫 이동, 위치 고정, 크기 조절 및 우클릭 빠른 메뉴를 제공한다. |
| FR-003 | Must | 영역 캡처, 현재 창 캡처, 클립보드 붙여넣기, 파일 선택, 파일·폴더 드롭을 지원한다. |
| FR-004 | Must | 캡처 조각이 펫에게 이동하고 처리 중 씹는 상태를 표현한다. Reduce Motion에서는 대체 표현을 사용한다. |
| FR-005 | Must | 자료를 먹인 뒤 지시 입력창 없이 즉시 처리한다. |
| FR-006 | Must | 스트리밍 답변, 입력 미리보기, 처리 Connector, 복사, 후속 질문, 실행, 닫기를 제공한다. |
| FR-007 | Must | 긴 답변은 말풍선에 요약하고 전체 내용은 확장 패널에서 제공한다. |
| FR-008 | Must | 일반 캡처·텍스트는 검증된 기본 에이전트에 전달하며 선택 경로가 unavailable이면 Task를 만들지 않는다. |
| FR-009 | Must | 파일·폴더·대용량·민감정보 입력은 전송 전 범위를 확인받는다. |
| FR-010 | Must | 설정에서 모든 전송을 매번 확인하는 옵션을 제공한다. |
| FR-011 | Should | URL을 클립보드 또는 드롭으로 인식하고 선택한 기본 Connector에 전달한다. |
| FR-012 | Must | 로그인 시 자동 실행 설정을 제공한다. |

**예외 흐름**

- 지원하지 않는 입력 형식은 전송하지 않고 형식과 실패 이유를 표시한다.
- 클립보드가 비어 있으면 Task를 만들지 않는다.
- 캡처가 취소되면 임시 이미지와 Task를 만들지 않는다.
- 원본 미리보기를 생성할 수 없어도 안전 검사를 건너뛰고 자동 전송하지 않는다.

### 25.2 활성 창 자동 맥락

| ID | 우선순위 | 요구사항 |
|---|---|---|
| FR-020 | Must | 먹이기 시점에만 현재 활성 창 맥락 수집을 시도한다. |
| FR-021 | Must | 앱 이름, 번들 ID, 창 제목, URL, 선택 텍스트, 포커스 요소와 주변 레이블, 현재 창 본문을 가능한 범위에서 수집한다. |
| FR-022 | Must | 다른 앱, 백그라운드 창, 다른 Space 및 보안 입력의 내용은 수집하지 않는다. |
| FR-023 | Must | 접근성 권한이 없으면 먹인 자료만 처리한다. |
| FR-024 | Must | 설정에서 자동 맥락 수집을 비활성화할 수 있다. |
| FR-025 | Must | 수집한 맥락에도 동일한 민감정보 정책을 적용한다. |
| FR-026 | Must | 활성 창 맥락 수집 실패가 먹인 자료 처리 자체를 막지 않는다. |

**예외 흐름**

- URL 또는 본문 속성을 제공하지 않는 앱에서는 얻을 수 있는 필드만 첨부한다.
- 보안 입력 요소가 감지되면 해당 요소의 값과 주변에서 비밀로 오인될 수 있는 값은 Meal에서 제외한다.
- 먹이기와 활성 앱 전환이 경합하면 먹이기 트리거 시점에 확정한 활성 앱만 대상으로 한다.

### 25.3 Meal·Task·FIFO 큐

| ID | 우선순위 | 요구사항 |
|---|---|---|
| FR-030 | Must | 한 번의 다중 선택 또는 다중 드롭으로 들어온 FoodItem만 하나의 Meal로 묶는다. |
| FR-031 | Must | 별도 먹이기 동작은 별도 Meal과 Task를 만든다. |
| FR-032 | Must | 시간 간격을 기준으로 Meal을 자동 병합하지 않는다. |
| FR-033 | Must | 동시에 하나의 Task만 실행한다. |
| FR-034 | Must | 실행 중 추가 Task는 FIFO 큐 끝에 추가한다. |
| FR-035 | Must | 대기 Task를 개별 취소할 수 있다. |
| FR-036 | Must | 현재 Task 종료 후 다음 Task를 자동 시작한다. |
| FR-037 | Must | 현재 Task의 상태와 대기 작업 수를 표시한다. |

**예외 흐름**

- 대기 Task 취소 시 Connector를 호출하지 않고 로컬 데이터만 삭제한다.
- 현재 Task 취소 시 Connector가 취소를 지원하지 않으면 로컬 출력 수신을 중단하고 프로세스 종료 가능성을 별도로 처리한다(TBD).
- 앱 종료 시 실행·대기 Task를 복구하지 않고 임시 원본을 다음 실행에서 삭제한다. 작업 복구는 MVP 제외다.

### 25.4 로컬 AgentConnector와 승인

| ID | 우선순위 | 요구사항 |
|---|---|---|
| FR-040 | Must | 설치된 Hermes, OpenCode, Codex, Claude Code를 검증된 정확 경로와 전용 비-shell argv로 실행한다. |
| FR-041 | Must | YumYum은 Keychain, 인증 토큰, 로그인 파일을 읽거나 별도 저장하지 않는다. |
| FR-042 | Must | 각 Connector의 진행, 텍스트, 승인, 실행, 결과, 오류를 공통 상태로 변환한다. |
| FR-043 | Must | 캘린더 일정 등록안을 실제 실행과 분리해 표시한다. |
| FR-044 | Must | 캘린더 등록 실행 직전에 항상 별도 승인을 받는다. |
| FR-045 | Must | 승인 화면에 변경 대상과 일정 필드를 표시한다. |
| FR-046 | Must | 승인 거부 시 외부 변경을 수행하지 않는다. |
| FR-047 | Must | 결과 성공 여부와 Hermes가 반환한 결과 요약을 표시한다. |
| FR-048 | Must | 승인 대기 중 다른 Task를 시작하지 않는다. |
| FR-049 | Must | 실행 결과가 불명확한 외부 변경을 자동 재시도하지 않는다. |

**예외 흐름**

- 선택한 CLI의 경로·버전·도움말 계약이 불일치하면 실행하지 않고 재선택 이유를 표시한다.
- 일정 필수 값이 없거나 복수 해석이면 승인 화면 대신 확인 질문을 표시한다.
- 승인 후 CLI가 실패하면 등록 성공으로 표시하지 않는다.
- 타임아웃 후 등록 여부가 불명확하면 “결과 확인 필요”로 표시하고 자동 재실행하지 않는다.

### 25.5 설정과 기록

| ID | 우선순위 | 요구사항 |
|---|---|---|
| FR-050 | Must | 네 CLI의 상태·정확 경로·버전, 기본 선택, 전역 단축키와 안전 정책을 설정에서 확인한다. |
| FR-051 | Must | 기본 기록에는 입력 유형, 처리 시각, 결과 요약만 저장한다. |
| FR-052 | Must | 사용자가 명시적으로 선택한 입력만 앱 전용 기록에 원본을 보관한다. |
| FR-053 | Must | 사용자는 저장된 원본과 기록을 삭제할 수 있다. |
| FR-054 | Must | 최근 먹이에서 보관되지 않은 원본을 다시 열 수 있는 것처럼 표시하지 않는다. |

---

## 26. 제품 상태·오류·권한 계층

### 26.1 Task 상태

```text
queued
  ├── cancelled
  └── inspecting
        ├── blocked
        ├── awaitingTransferConfirmation
        │     ├── cancelled
        │     └── sending
        └── sending
              ├── failed
              └── processing
                    ├── awaitingApproval
                    │     ├── cancelled
                    │     └── executing
                    ├── completed
                    ├── failed
                    └── cancelling
                          └── cancelled
```

펫의 `idle`, `hover`, `ready`, `catch`, `chew`, `think`, `answer`, `action`, `success`, `error`, `blocked`는 Task 상태를 시각화한다. 데이터 상태의 기준은 Task 상태이며 애니메이션 자체가 상태 원장은 아니다.

### 26.2 오류 분류

| 코드군 | 분류 | 사용자 복구 |
|---|---|---|
| INPUT | 빈 입력, 미지원 형식, 읽기 실패 | 다른 입력 선택 또는 재시도 |
| PERMISSION | 화면 캡처, 접근성, 파일 접근 거부 | 설정 안내 또는 제한 모드 계속 |
| PRIVACY | 절대 차단, 일반 개인정보, 안전 검사 실패 | 제외 범위 확인 또는 취소 |
| CONNECTOR | Hermes 미설치, 미로그인, 버전 불일치 | Hermes 설치·로그인·업데이트 |
| PROCESS | 자식 프로세스 시작·종료·출력 파싱 실패 | 재시도 또는 진단 정보 확인 |
| NETWORK | Hermes 내부 연결 실패 | 연결 확인 후 재시도 |
| APPROVAL | 승인 만료, 거부, 불완전 등록안 | 수정 요청 또는 취소 |
| EXECUTION | 캘린더 등록 실패·결과 불명 | 결과 확인, 자동 재실행 금지 |
| STORAGE | 임시 저장·삭제 실패 | 처리 중단 또는 다음 실행 정리 |

### 26.3 권한과 동의 계층

1. **macOS 시스템 권한**: ScreenCaptureKit, Accessibility, 선택한 파일·폴더 접근
2. **처리 동의**: 먹이기 행위로 해당 자료와 현재 활성 창 관련 맥락 처리 동의
3. **전송 확인**: 민감정보, 파일·폴더, 대용량 또는 모든 전송 확인 옵션에 따라 요청
4. **외부 변경 승인**: 캘린더 일정 등록 직전 항상 별도 요청
5. **범위 확대 승인**: MVP에서 승인 범위를 다른 작업·세션으로 재사용하지 않음

권한은 필요한 기능을 처음 사용할 때 요청한다. 권한 거부를 앱 전체 실패로 취급하지 않고 가능한 제한 모드로 계속한다.

---

## 27. 데이터 모델과 생명주기

### 27.1 FoodItem

```text
FoodItem
├── id
├── type: screenshot | image | selectedText | clipboardText | file | folder | url | accessibilityContext
├── sourceApplication
├── sourceBundleID
├── sourceWindowTitle
├── sourceURL
├── localTemporaryReference
├── retainedReference?
├── extractedText
├── mimeType
├── byteSize
├── createdAt
├── sensitivityFindings
├── excludedRanges
└── retentionChoice: temporary | retained
```

`sourceWindowTitle`, 원본 파일명과 원문은 진단 로그·분석 데이터에 기록하지 않는다. 데이터 모델에 존재하는 것과 관측성 전송 허용은 별개다.

### 27.2 Meal

```text
Meal
├── id
├── items: [FoodItem]
├── trigger: capture | paste | drop | filePicker
├── createdAt
├── privacySummary
├── transferConfirmationRequired
├── activeWindowContextIncluded
└── selectedConnector: hermesLocal
```

MVP는 별도 사용자 프롬프트를 받지 않는다. Hermes에 전달할 기본 의도는 “이 자료를 이해하고 가장 유용한 답변 또는 실행 가능한 등록안을 제안”하는 제품 명령으로 고정한다. 구체 문구는 **TBD**다.

### 27.3 Task

```text
Task
├── id
├── mealID
├── state
├── queuePosition
├── connectorTaskID?
├── sessionReference?
├── createdAt
├── startedAt?
├── finishedAt?
├── approval?
├── resultSummary?
└── error?
```

### 27.4 생명주기

| 단계 | 원본 | 추출·민감정보 결과 | 기록 |
|---|---|---|---|
| 수집 | 앱 전용 임시 디렉터리 또는 메모리 | 로컬 생성 | Task ID 생성 |
| 검사 | 처리에 필요한 동안만 유지 | 전송 범위 계산 | 콘텐츠 로그 금지 |
| 처리 | Task 완료·취소 전까지 유지 | Hermes 전달 범위만 사용 | 상태 이벤트 기록 |
| 완료·취소 | 즉시 삭제 | 메모리 객체 즉시 해제 | 입력 유형·시각·결과 요약 저장 |
| 비정상 종료 | 잔여 가능 | 프로세스 종료와 함께 소멸 | 다음 실행 정리 이벤트 |
| 다음 실행 | 잔여 임시 파일 자동 삭제 | 없음 | 삭제 성공 여부만 기록 |
| 명시적 보관 | 사용자가 선택한 원본만 앱 전용 기록에 이동 | 필요한 메타데이터 | 사용자 삭제 전까지 보관 |

원본 자동 보관 기간 설정과 작업 복구는 MVP에서 제외한다.

---

## 28. 민감정보 정책

### 28.1 절대 전송 금지

- 개인 키
- 복구 키
- 인증서 비밀키
- API 키
- 액세스 토큰
- 세션 쿠키
- 비밀번호
- 보안 입력 필드 내용
- `.env` 등 자격증명 파일

절대 차단 정보는 Meal, Hermes 입력, 진단 로그 및 분석 데이터에 포함하지 않는다. MVP에서는 사용자 강제 전송을 허용하지 않는다.

### 28.2 일반 개인정보

- 주민등록번호
- 계좌번호
- 카드번호
- 전화번호
- 이메일 주소

일반 개인정보가 탐지되면 기본 마스킹 미리보기와 경고를 표시한다. 사용자가 전송할 파일과 구간을 명시적으로 확인한 경우에만 해당 범위를 전송한다.

### 28.3 부분 제외

- 일부 파일에서 탐지되면 해당 파일만 제외할 수 있다.
- 일부 구간에서 탐지되면 해당 구간을 마스킹 또는 제외할 수 있다.
- 안전한 나머지 자료는 계속 처리한다.
- 전송 전 제외 항목, 개수 및 이유를 표시한다.
- 안전 검사 자체가 실패하면 자동 전송하지 않는다.

### 28.4 MVP 이후

- 오탐 신고
- 사용자 정의 영구 예외 규칙
- 조직 정책 및 관리형 차단 규칙

---

## 29. Local CLI Connectors

### 29.1 책임 경계

**YumYum 책임**

- 네 지원 CLI의 안전한 직접 후보 및 사용자 명시 경로 확인
- 정확 경로의 실행 권한, 버전과 로컬 도움말 계약 확인
- 로컬 자식 프로세스 시작·관찰·종료
- Meal의 안전한 입력 범위 구성
- 각 전용 Connector 출력을 공통 상태로 변환
- Task 큐와 UI 상태 관리
- 승인 화면 표시와 사용자 결정을 Connector에 전달
- 임시 원본 삭제 및 최소 기록 보존

**Hermes 책임**

- 사용자 요청과 자료 해석
- 일정 정보 추출과 등록안 생성
- 세션 및 기억 관리
- 캘린더 도구 선택과 실행
- 외부 서비스 인증
- 실행 결과 생성

**YumYum이 하지 않는 일**

- Hermes 토큰 수집·복사·저장
- 모델 또는 도구 선택
- 일정 내용을 독자적으로 추론해 수정
- Hermes의 기억을 별도 복제
- Hermes 대신 캘린더 API 직접 호출

### 29.2 공통 계약

```swift
protocol AgentConnector {
    var id: String { get }
    var displayName: String { get }
    var capabilities: Set<AgentCapability> { get }

    func checkConnection() async throws -> ConnectionStatus
    func send(_ meal: Meal) -> AsyncThrowingStream<AgentEvent, Error>
    func approve(_ approvalID: String) async throws
    func reject(_ approvalID: String) async
    func cancel(taskID: String) async
}
```

개념 계약이며 실제 Swift 시그니처는 구현 설계에서 확정한다. 외부 변경 승인 계약이 확인되지 않으면 해당 요청을 차단하며 임의 프로토콜이나 자동 재호출을 설계하지 않는다.

### 29.3 CLI 계약 검증 항목

| ID | 상태 | 검증 항목 |
|---|---|---|
| H-TBD-001 | TBD | 공식 설치 경로, 실행 파일 탐색 및 최소 지원 CLI 버전 |
| H-TBD-002 | TBD | 비대화형 실행 방식과 표준 입력·인자 전달 규격 |
| H-TBD-003 | TBD | JSON 또는 기타 구조화 출력 지원 여부와 스키마 |
| H-TBD-004 | TBD | 토큰 단위 또는 이벤트 단위 스트리밍 지원 여부 |
| H-TBD-005 | TBD | 세션 생성·재사용·후속 질문 식별 방식 |
| H-TBD-006 | TBD | 도구 요청과 실행 전 승인 분리 지원 여부 |
| H-TBD-007 | TBD | 실행 중 취소 신호와 안전한 프로세스 종료 방식 |
| H-TBD-008 | TBD | 캘린더 등록 결과와 외부 식별자 반환 여부 |
| H-TBD-009 | TBD | 파일·이미지·폴더 입력의 Connector별 전달 방식과 지원 형식 |
| H-TBD-010 | TBD | 종료 코드, stderr, 인증 만료 및 네트워크 오류 규격 |
| H-TBD-011 | TBD | 자식 프로세스별 환경변수 상속과 비밀정보 노출 방지 |
| H-TBD-012 | TBD | CLI 업데이트에 따른 호환성 탐지 방식 |

구조화 출력, 스트리밍, 세션, 승인, 취소는 공식 문서와 실제 CLI 검증 전까지 제품 확정 기능이 아니라 Connector 검증 게이트다.

---

## 30. 캘린더 일정 등록 승인 E2E

### 30.1 정상 흐름

1. 사용자가 일정 정보가 담긴 캡처·이미지·텍스트를 먹인다.
2. YumYum이 활성 창 맥락을 허용 범위에서 수집하고 민감정보를 검사한다.
3. 확인이 필요하지 않은 일반 입력은 즉시 Hermes에 전달한다.
4. Hermes가 일정 등록안을 구조화된 형태로 반환한다.
5. YumYum이 다음 필드를 표시한다.
   - 제목
   - 날짜
   - 시작·종료 시각
   - 시간대
   - 대상 캘린더
   - 장소
   - 메모
6. 사용자가 `등록` 또는 `취소`를 선택한다.
7. `등록` 선택 시 YumYum이 Hermes에 해당 승인 ID의 실행을 요청한다.
8. Hermes가 캘린더 도구를 실행하고 결과를 반환한다.
9. YumYum은 성공 여부와 결과 요약을 표시한다.
10. 원본을 삭제하고 입력 유형·처리 시각·결과 요약만 기록한다.

### 30.2 승인 규칙

- 등록안 생성은 외부 변경이 아니므로 별도 실행 승인 전까지 허용한다.
- 실제 캘린더 변경은 항상 별도 승인한다.
- 승인에는 표시된 일정 1건과 현재 실행 1회만 포함한다.
- 승인 내용을 변경한 뒤 자동 실행하지 않는다. 변경된 등록안을 다시 표시한다.
- 승인 대기 중 시간 초과 기준은 **TBD**다.

### 30.3 예외 흐름

| 상황 | 동작 |
|---|---|
| 날짜·시간 불명확 | 등록하지 않고 Hermes 확인 질문 표시 |
| 시간대 불명확 | 추정값을 자동 실행하지 않고 등록안에서 확인 요구 |
| 캘린더 미지정 | Hermes 기본값을 표시하되 사용자가 승인 화면에서 확인 |
| 중복 일정 가능성 | Hermes가 제공할 경우 경고 표시. 탐지 지원 여부는 TBD |
| 승인 거부 | 외부 변경 없이 종료 |
| 실행 실패 | 실패 이유와 재시도 제공 |
| 결과 불명 | 자동 재실행 금지, 캘린더 확인 안내 |
| Task 취소 | 가능한 경우 Hermes 취소, 원본 삭제 |

---

## 31. 비기능 요구사항과 성능 예산

### 31.1 확정 요구사항

| ID | 요구사항 |
|---|---|
| NFR-001 | 대기 상태 네트워크 요청은 없어야 한다. 연결 상태 수동 확인과 업데이트 확인 정책은 TBD다. |
| NFR-002 | 대기 중 상시 화면 캡처, OCR, Vision 처리, 접근성 본문 수집을 하지 않는다. |
| NFR-003 | 대기 애니메이션은 정지 또는 이벤트 기반 저빈도로 실행하며 상시 60fps를 사용하지 않는다. |
| NFR-004 | 입력 원본은 완료·취소 후 즉시 삭제하고 관련 메모리 객체를 해제한다. |
| NFR-005 | macOS 14에서 제공되는 ScreenCaptureKit, Vision, Accessibility API 범위만 사용한다. |
| NFR-006 | Intel Mac용 별도 우회 구현을 제공하지 않는다. |
| NFR-007 | 다중 모니터, Spaces 및 전체 화면 앱에서 펫·캡처·말풍선 핵심 흐름을 검증한다. |
| NFR-008 | 오류가 데이터 유실 또는 중복 외부 변경으로 이어지지 않게 한다. |

### 31.2 성능 예산

| 지표 | 목표 |
|---|---|
| 대기 CPU | 0%에 가까운 수준. 합격 상한과 측정 구간 **TBD** |
| 대기 메모리 | 기존 목표 30~70MB. 최종 합격 상한 **TBD** |
| 대기 네트워크 | 0 요청/바이트. 예외 정책 **TBD** |
| 펫 클릭·드롭 시각 반응 | **TBD ms 이하** |
| 캡처 종료 후 로컬 미리보기 | **TBD ms 이하** |
| 일반 입력 후 Hermes 프로세스 시작 | **TBD ms 이하** |
| Hermes 첫 스트림 이벤트 | Hermes 자체 시간을 분리 측정, 목표 **TBD** |
| 취소 버튼 로컬 반응 | **TBD ms 이하** |
| 완료·취소 후 임시 파일 삭제 | **TBD 초 이내** |
| 최대 단일 파일 크기 | **20MB** |
| 최대 Meal 전체 크기 | **TBD MB** |
| 최대 폴더 파일 수·스캔 시간 | **TBD 개 / TBD 초** |
| 최대 FIFO 대기 작업 수 | **TBD** |
| 장시간 안정성 시험 | **TBD 시간** 연속 실행 |

나머지 수치는 Hermes CLI 검증과 상호작용 프로토타입 계측 후 확정한다. TBD 수치가 확정되기 전 성능 수용 판정을 하지 않는다.

### 31.3 호환성과 배포

- 최소 지원: macOS 14 Sonoma
- 검증 대상: macOS 14, macOS 15, macOS 26
- 하드웨어: Apple Silicon
- 주요 검증 환경: macOS 26
- Intel Mac: MVP 공식 지원 제외
- 서명·공증·자동 업데이트 방식: **TBD**
- Mac App Store 배포: MVP 제외

---

## 32. 접근성

| ID | 요구사항 |
|---|---|
| A11Y-001 | 펫, 빠른 메뉴, 말풍선, 큐, 승인 화면의 모든 조작 요소에 VoiceOver 이름·역할·상태를 제공한다. |
| A11Y-002 | 상태를 색상, 표정 또는 애니메이션만으로 전달하지 않고 텍스트 또는 접근성 알림을 병행한다. |
| A11Y-003 | Reduce Motion 활성화 시 날아가기·씹기·반복 이동을 정적 또는 페이드 상태 변화로 대체한다. |
| A11Y-004 | 캡처 영역 선택을 제외한 입력, 결과 확인, 복사, 승인, 취소, 설정 흐름을 키보드로 수행할 수 있다. |
| A11Y-005 | 시스템 텍스트 크기와 충분한 대비를 지원한다. 구체 기준은 Apple 접근성 지침 검증 후 확정한다. |
| A11Y-006 | 기본 대기 상태에서 활성 앱의 키보드 포커스를 빼앗지 않는다. |
| A11Y-007 | 승인 요청·실패·차단·완료 상태를 VoiceOver에 알린다. |
| A11Y-008 | 클릭 통과 또는 자동 숨김 상태에서도 키보드나 메뉴 막대 등 복구 가능한 접근 경로를 제공한다. |

---

## 33. 관측성

### 33.1 허용 이벤트

- 앱 시작·정상 종료·비정상 종료 감지
- 권한 요청·허용·거부
- 입력 유형과 FoodItem 개수
- Task 상태 전환과 큐 대기 시간
- Connector 연결·프로세스 시작·종료 코드
- 첫 이벤트와 완료까지의 지연
- 승인 표시·승인·거부
- 작업 성공·실패·취소
- 민감정보 분류별 탐지 개수와 제외 개수
- 임시 파일 정리 성공·실패
- CPU·메모리 샘플

### 33.2 금지 데이터

- 입력 원본 내용
- 파일명
- 창 제목
- 선택 텍스트와 접근성 본문
- URL 원문
- 프롬프트·답변 원문
- Hermes 인증정보
- 탐지된 민감정보 원문

### 33.3 운영 원칙

- Task별 무작위 correlation ID를 사용한다.
- 진단 로그와 제품 분석 데이터는 분리한다.
- 분석 데이터 수집 기본값과 동의 방식은 **TBD**다.
- 사용자가 진단 로그를 확인·내보내기·삭제할 수 있는 범위는 **TBD**다.
- 로그 보존 기간은 **TBD**다.

---

## 34. MVP Acceptance Criteria

### 34.1 제품 경계

- AC-001: 앱은 Swift와 AppKit 기반 네이티브 macOS 독립 앱으로 동작한다.
- AC-002: 펫과 말풍선 핵심 경험에 Electron 또는 상시 WebView를 사용하지 않는다.
- AC-003: YumYum은 자체 모델, 장기 기억, 도구 선택 또는 실행 엔진을 제공하지 않는다.
- AC-004: MVP의 모든 에이전트 작업은 선택한 Hermes, OpenCode, Codex, Claude Code 전용 Connector 중 하나를 통과한다.
- AC-005: Connector 교체가 펫과 말풍선 상태 UI의 재구현을 요구하지 않는다.

### 34.2 명시적 입력과 맥락

- AC-010: 먹이기 없이 화면·클립보드·접근성 본문을 수집하거나 처리하지 않는다.
- AC-011: 먹이기 시 현재 활성 창의 허용된 맥락만 수집한다.
- AC-012: 다른 앱, 백그라운드 창, 다른 Space 및 보안 입력을 수집하지 않는다.
- AC-013: 접근성 권한이 없으면 먹인 자료만으로 Task를 계속 처리한다.
- AC-014: 사용자는 자동 맥락 수집을 설정에서 끌 수 있다.
- AC-015: 맥락 수집 실패가 먹인 자료 처리 실패로 이어지지 않는다.
- AC-016: 자료를 먹이면 별도 지시 입력창 없이 처리 또는 필수 전송 확인을 즉시 시작한다.

### 34.3 입력·Meal·큐

- AC-020: 영역 캡처, 현재 창 캡처, 클립보드 텍스트·이미지, 파일·폴더 드롭과 파일 선택을 지원한다.
- AC-021: 한 번의 다중 선택 또는 다중 드롭만 하나의 Meal을 만든다.
- AC-022: 별도 먹이기는 시간 간격과 관계없이 별도 Meal과 Task를 만든다.
- AC-023: 한 번에 하나의 Task만 실행한다.
- AC-024: 실행 중 추가 Task는 FIFO 순서로 대기한다.
- AC-025: 사용자는 대기 Task를 개별 취소할 수 있다.
- AC-026: 현재 Task 종료 후 취소되지 않은 다음 Task가 자동 시작한다.
- AC-027: 현재 상태와 대기 작업 수를 확인할 수 있다.

### 34.4 전송과 민감정보

- AC-030: 일반 캡처·텍스트는 명시적으로 선택한 검증된 기본 로컬 에이전트에 전달한다.
- AC-031: 파일·폴더·대용량 자료는 전송 전에 범위를 표시하고 확인받는다.
- AC-032: 모든 전송을 매번 확인하도록 설정할 수 있다.
- AC-033: 개인 키, 복구 키, 인증서 비밀키, API 키, 액세스 토큰, 세션 쿠키, 비밀번호, 보안 입력 및 자격증명 파일은 전송되지 않는다.
- AC-034: 절대 차단 정보는 사용자 승인으로도 MVP에서 전송할 수 없다.
- AC-035: 일반 개인정보가 탐지되면 마스킹 미리보기와 경고를 표시한다.
- AC-036: 일반 개인정보 원문은 사용자가 전송 범위를 명시적으로 확인한 경우에만 전달한다.
- AC-037: 일부 파일·구간에서 민감정보가 발견되면 해당 범위만 제외하고 안전한 나머지를 처리할 수 있다.
- AC-038: 제외 항목, 개수 및 이유를 표시한다.
- AC-039: 민감정보 안전 검사 실패 시 자동 전송하지 않는다.
- AC-040: 오탐 신고와 영구 예외 규칙을 MVP에 제공하지 않는다.

### 34.5 로컬 에이전트 발견·연결

- AC-050: 네 지원 CLI를 안전한 고정 디렉터리와 사용자 명시 절대 경로에서만 발견한다.
- AC-051: YumYum은 Keychain, 토큰, 로그인 파일을 읽거나 별도 보관하지 않는다.
- AC-052: 실행 권한, 버전과 전용 로컬 도움말 계약을 timeout·최소 환경·출력 상한으로 확인한다.
- AC-053: 선택 경로 실패 시 다른 설치·정의·원격 서비스로 자동 폴백하지 않는다.
- AC-054: 설정에 모든 정의의 상태·정확 경로·버전을 표시하고 기본 에이전트를 선택할 수 있다.
- AC-055: 앱 시작, 재검색, 빠른 메뉴 열기, 전송 직전에 재검증하며 unavailable 이후 명시적 재선택을 요구한다.

### 34.6 캘린더 등록

- AC-060: 일정 자료를 먹이면 Hermes가 생성한 등록안을 실제 실행 전에 표시한다.
- AC-061: 등록안은 제목, 날짜, 시작·종료 시각, 시간대, 캘린더, 장소, 메모의 제공 여부를 표시한다.
- AC-062: 필수 값이 불명확하면 실제 등록하지 않고 확인 질문을 표시한다.
- AC-063: 실제 캘린더 등록 직전에 항상 별도 승인을 요청한다.
- AC-064: 승인은 표시된 일정 1건의 현재 실행 1회에만 적용된다.
- AC-065: 사용자가 거부하면 캘린더를 변경하지 않는다.
- AC-066: 승인 후 Hermes의 성공 결과를 받은 경우에만 성공으로 표시한다.
- AC-067: 결과가 불명확하면 자동 재실행하지 않고 확인 필요 상태를 표시한다.
- AC-068: 승인 대기 중 다음 FIFO Task를 시작하지 않는다.

### 34.7 데이터 생명주기

- AC-070: 입력 원본은 처리 중에만 앱 전용 임시 디렉터리 또는 메모리에 둔다.
- AC-071: 작업 완료 또는 취소 후 임시 원본을 즉시 삭제한다.
- AC-072: 처리 종료 후 관련 이미지와 메모리 객체를 즉시 해제한다.
- AC-073: 앱 시작 시 이전 비정상 종료로 남은 임시 파일을 자동 삭제한다.
- AC-074: 기본 기록에는 입력 유형, 처리 시각, 결과 요약만 저장한다.
- AC-075: 사용자가 항목별로 명시적으로 보관한 원본만 앱 전용 기록에 저장한다.
- AC-076: 사용자가 보관 원본과 기록을 삭제할 수 있다.
- AC-077: 원본 자동 보관 기간 설정은 MVP에 포함하지 않는다.
- AC-078: 로그와 분석 데이터에 원본, 파일명, 창 제목, URL 및 민감정보 원문을 기록하지 않는다.

### 34.8 UI·상태·접근성

- AC-080: 펫은 투명 창으로 표시되고 기본적으로 Dock에 나타나지 않는다.
- AC-081: 펫을 이동하고 위치를 유지할 수 있다.
- AC-082: 펫 클릭과 기본 `Control+Option+Space`가 펫 옆 말풍선을 열며 입력 조작 전 활성 앱의 포커스를 임의로 빼앗지 않는다.
- AC-083: 입력, 처리, 승인, 실행, 성공, 실패, 차단 상태를 구분한다.
- AC-084: 상태는 캐릭터 표정이나 색상만으로 전달하지 않는다.
- AC-085: 긴 답변은 말풍선에 요약하고 전체 답변 접근 경로를 제공한다.
- AC-086: 답변 복사와 후속 질문을 제공한다.
- AC-087: VoiceOver가 주요 조작 요소, 상태, 승인 내용과 오류를 읽을 수 있다.
- AC-088: Reduce Motion에서 이동·씹기 애니메이션을 비움직임 대체 표현으로 바꾼다.
- AC-089: 캡처 선택을 제외한 핵심 흐름을 키보드와 VoiceOver로 조작할 수 있고 전역 단축키를 설정에서 변경할 수 있다.

### 34.9 성능·호환성

- AC-090: 대기 상태에서 화면 캡처, OCR, 접근성 본문 수집을 실행하지 않는다.
- AC-091: 대기 상태 네트워크 사용은 확정된 예외가 없는 한 0이다.
- AC-092: 대기 애니메이션은 상시 60fps로 실행하지 않는다.
- AC-093: 대기 메모리와 CPU가 확정될 성능 예산을 충족한다.
- AC-094: macOS 14·15·26 Apple Silicon에서 빌드와 핵심 사용자 흐름 테스트를 통과한다.
- AC-095: Intel Mac 미지원 사실을 배포 요구사항에 명시한다.
- AC-096: 다중 모니터와 전체 화면 앱에서 펫, 캡처, 말풍선이 핵심 흐름을 완료한다.
- AC-097: 앱 비정상 종료 후 다음 실행에서 임시 원본 정리 검증을 통과한다.

### 34.10 MVP 제외 확인

- AC-100: 상시 화면 기록, 장기 화면 기억, 능동 제안을 기본 기능으로 제공하지 않는다.
- AC-101: Live2D, 3D, 성장·소셜 기능, 음성 대기, 플러그인 마켓을 포함하지 않는다.
- AC-102: Webhook·OpenAI 호환 API·임의 플러그인·사용자 명령 Connector를 MVP에 제공하지 않는다.
- AC-103: 시간 기반 Meal 병합, 동시 Task 실행, 작업 복구를 제공하지 않는다.
- AC-104: 민감정보 강제 전송, 오탐 신고, 영구 예외 규칙을 제공하지 않는다.

---

## 35. 테스트 매트릭스

### 35.1 플랫폼 매트릭스

| 환경 | 하드웨어 | 빌드 | 캡처·텍스트·파일 | 빠른 메뉴·단축키 | 네 로컬 CLI | 승인·캘린더 | 큐·취소 | 데이터 정리 |
|---|---|---:|---:|---:|---:|---:|---:|---:|
| macOS 14 Sonoma | Apple Silicon | 필수 | 필수 | 필수 | 필수 | 필수 | 필수 | 필수 |
| macOS 15 | Apple Silicon | 필수 | 필수 | 필수 | 필수 | 필수 | 필수 | 필수 |
| macOS 26 | Apple Silicon | 필수·주요 | 필수·주요 | 필수·주요 | 필수·주요 | 필수·주요 | 필수·주요 | 필수·주요 |
| Intel Mac | Intel | 미지원 | 미지원 | 미지원 | 미지원 | 미지원 | 미지원 | 미지원 |

### 35.2 핵심 사용자 흐름

각 지원 OS에서 다음을 검증한다.

1. 최초 실행과 네 CLI 발견·버전·경로·상태 확인 및 기본 선택
2. 펫 클릭·전역 단축키 → 화면 캡처/텍스트/다중 파일 → 선택 에이전트 답변
3. 접근성 권한 거부 → 먹인 자료만 처리
4. 일반 개인정보 → 마스킹 미리보기 → 범위 확인
5. 절대 차단 정보 → 전송 불가
6. 파일·폴더 → 범위 확인 → 제외 항목 표시
7. 실행 중 추가 먹이기 → FIFO → 대기 취소 → 자동 다음 시작
8. 일정 등록안 → 별도 승인 → 실제 등록 결과
9. 승인 거부 → 외부 변경 없음
10. 비정상 종료 → 다음 실행 임시 파일 삭제
11. 다중 모니터·Spaces·전체 화면
12. VoiceOver·키보드·Reduce Motion

네 CLI 버전 조합과 로컬 도움말 계약은 지원 매트릭스에 함께 기록한다.

---

## 36. 단계별 출시 계획과 게이트

### 0단계: 로컬 CLI 계약·권한 위험 해소

- 네 CLI 설치·버전·도움말·입출력·취소·승인 조사
- macOS 14 기준 ScreenCaptureKit·Vision·Accessibility 검증
- 민감정보 분류와 임시 파일 삭제 프로토타입

**진입 게이트:** 없음
**완료 게이트:** 네 전용 실행 계약과 Hermes ACP 지원 여부 기록, 승인 없는 외부 변경 차단 확인

### 1단계: 인터랙션 프로토타입

- 투명 펫 창, 이동, 영역 캡처
- 먹기·씹기·말풍선
- 가짜 AgentEvent와 승인 화면
- Reduce Motion 대체 표현

**완료 게이트:** 무설명 먹이기 과업과 포커스 비침해 검증

### 2단계: 로컬 에이전트 답변형 세로 슬라이스

- Hermes ACP, OpenCode run, Codex exec, Claude print Connector
- 캡처·텍스트 전달
- 스트리밍 또는 검증된 최소 출력 방식
- 후속 질문과 복사

**완료 게이트:** macOS 14·15·26에서 입력부터 답변까지 통과

### 3단계: 범용 입력·보호·큐

- 이미지, 파일, 폴더, URL
- FoodItem·Meal·Task와 FIFO
- 민감정보 차단·마스킹·범위 확인
- 데이터 삭제와 기록

**완료 게이트:** 절대 차단 우회 불가, 부분 제외, 큐·취소, 비정상 종료 정리 통과

### 4단계: 캘린더 실행

- 일정 등록안
- 실행 직전 승인
- 결과·실패·불명 상태

**완료 게이트:** 승인 없는 외부 변경 0건, E2E 수용 기준 통과

### 5단계: 제품화

- 온보딩, 권한 안내, 로그인 실행
- 서명·공증·자동 업데이트
- 성능 예산 확정과 최적화
- 접근성·관측성·장시간 안정성

**완료 게이트:** 전체 AC와 테스트 매트릭스 통과, 출시 차단 결함 0건

### 후속 단계

- Webhook·OpenAI 호환 API Connector
- 자동 라우팅
- 선택형 Hermes Desktop 플러그인
- 장기 화면 기억, 능동 제안, 펫 확장

---

## 37. 위험과 검증 실험

| ID | 위험 | 검증 실험 | 성공 기준 |
|---|---|---|---|
| R-001 | 먹이기 동작을 사용자가 발견하지 못함 | 설명 없는 캡처·드롭 과업 테스트 | 목표 성공률 **TBD** |
| R-002 | 즉시 처리 때문에 잘못된 자료가 전송됨 | 일반 입력 즉시 전송과 매번 확인 옵션 비교 | 취소·후회 비율 목표 **TBD** |
| R-003 | 자동 활성 창 맥락이 과도하게 수집됨 | 앱별 수집 필드 감사와 사용자 이해도 테스트 | 백그라운드·보안 입력 수집 0건 |
| R-004 | 민감정보 오탐·미탐 | 대표 자격증명·개인정보 corpus 검사 | 차단·탐지 목표치 **TBD** |
| R-005 | Hermes CLI가 승인·스트리밍을 지원하지 않음 | 공식 문서와 실제 CLI spike | 안전한 캘린더 E2E 가능 여부 결정 |
| R-006 | 취소 후 자식 프로세스나 외부 작업이 계속됨 | 단계별 취소·프로세스 종료 시험 | 안전 상태 판별 가능 |
| R-007 | 캘린더 중복 등록 | 타임아웃·스트림 단절·재시도 시험 | 결과 불명 시 자동 재실행 0건 |
| R-008 | 펫이 업무를 방해함 | 하루 종일 사용과 전체 화면·다중 모니터 관찰 | 숨김·이동·포커스 침해 목표 **TBD** |
| R-009 | 대형 폴더가 자원을 과다 사용함 | 파일 수·용량별 로컬 스캔 계측 | 성능 예산 확정 |
| R-010 | 애니메이션이 대기를 늘리거나 접근성을 해침 | 애니메이션/Reduce Motion 비교 | 과업 시간 악화 목표 **TBD** |
| R-011 | macOS 버전별 API 동작 차이 | 14·15·26 동일 시나리오 회귀 | 핵심 흐름 통과율 100% |
| R-012 | 장난감으로 인식되어 업무 신뢰가 낮음 | 캘린더 실행형 데모와 답변형 데모 비교 | 신뢰·재사용 의향 목표 **TBD** |

---

## 38. 원문 요구사항 추적표

| 원문 장 | 핵심 요구사항 | 상세 PRD 반영 |
|---|---|---|
| 1. 제품 개요 | macOS 펫, 먹이기, 답변과 실제 작업 | 21, 24, 25, 30 |
| 2. 해결하려는 문제 | 앱 전환·복붙·재실행 감소 | 22, 23, 37 |
| 3. 제품 콘셉트 | 먹이기 메타포, connector UI, 네이티브 독립 앱 | 21, 22, 25 |
| 4. 목표 사용자 | 지식 근로자, Hermes 사용자, 개발자 | 23 |
| 5. 핵심 사용자 경험 | 펫, 캡처, 드롭, 클립보드, 말풍선 | 24, 25, 34 |
| 6. 먹이 데이터 모델 | FoodItem, Meal | 27 |
| 7. 에이전트 연동 | Connector, Hermes 우선, Codex 후속 | 29, 36 |
| 8. 펫 상태·애니메이션 | 상태 표현, 입력별 애니메이션, 단순 2D | 26, 32, 34 |
| 9. 기술 구성 | Swift, AppKit, SwiftUI 설정, 네이티브 API | 21, 31 |
| 10. 성능 목표 | 저 CPU·메모리·네트워크, 이벤트 기반 처리 | 31, 34 |
| 11. 개인정보 보호와 안전 | 명시적 입력, 지연 권한, 파일 차단 | 26, 27, 28 |
| 12. MVP 범위 | 펫·입력·Hermes·스트리밍·후속 질문·자동 실행 | 25, 34, 36 |
| 13. 주요 화면 및 UI | 펫, 빠른 메뉴, 말풍선, 설정 | 25, 32 |
| 14. 후속 버전 아이디어 | 장기 기억, 능동 제안, 펫 확장 | 36 후속 단계 및 기존 14장 유지 |
| 15. 제품명과 언어 표현 | YumYum, 문구, 태그라인 | 기존 15장 유지 |
| 16. 차별점 | 범용 입력, 애니메이션, 실제 실행, 경량성 | 21, 22 |
| 17. 성공 판단 기준 | 사용량, 지연, 실행률, 자원, 통제감 | 31, 33, 37 |
| 18. 개발 단계 | 프로토타입부터 제품화 | 36 |
| 19. 예상 위험 | 권한, 방해, 대형 폴더, Connector, 신뢰 | 37 |
| 20. 최종 제품 정의 | 에이전트가 아닌 macOS 인터페이스 | 21 불변 조건 |
| 결정 D-001~010 | 캘린더, CLI, 즉시 처리, 맥락, 전송, 민감정보, 보존, OS, FIFO, Meal | 24~35 |

이 추적표는 기존 제품명, 태그라인, UX, 애니메이션, 기술 구성 및 후속 아이디어가 삭제되지 않았음을 확인하는 기준으로 사용한다.

---

## 39. 남은 TBD

### 출시 전 차단 TBD

1. H-TBD-001~012 Hermes CLI 실제 계약
2. Hermes 승인 분리 또는 안전한 대체 흐름의 가능 여부
3. 캘린더 등록 결과의 성공·실패·불명 판별 방식
4. 성능 예산의 수치와 측정 조건
5. 파일·Meal·폴더·FIFO 최대 한도
6. 민감정보 탐지 방식과 정확도 목표
7. 승인 대기·프로세스·응답 타임아웃

### 비차단 제품화 TBD

1. 서명·공증·자동 업데이트 방식
2. 분석 데이터 기본값과 동의 방식
3. 진단 로그 제공 범위와 보존 기간
4. 지원 파일 형식 목록
5. 기본 Hermes 요청 문구
6. 제품 성공 지표의 목표값, 평가 기간 및 표본 규모
7. UI 대비·텍스트 확대의 세부 검증 기준
